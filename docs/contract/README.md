Platform type definitions for the claude.ai Artifact runtime, contract 0.2.52. Copied verbatim; do not edit.
Only the capabilities this page uses (`db`, `sample`) plus the entry point (`claude.d.ts`) and `artifact.d.ts` (for a possible future move off manual republishing). If the contract is upgraded, replace these files and update `docs/RUNTIME.md`.
